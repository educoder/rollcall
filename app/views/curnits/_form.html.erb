<%= form_for(@curnit) do |f| %>
  <% if @curnit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curnit.errors.count, "error") %> prohibited this app from being saved:</h2>

      <ul>
      <% @curnit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <em>The directory on the server where the S3 application is installed.</em>
    <%= f.label :path %><br />
    <%= f.text_field :path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
